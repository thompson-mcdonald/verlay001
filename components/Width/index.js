import styled from "styled-components";
import { width, margin, color, padding, position } from "styled-system";

export const Width = styled.div`
  ${width}
  ${margin}
  ${color}
  ${padding}
  ${position}
`;
